import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        paddingTop: 100
    },
    logo: {
        height: 257,
        maxWidth: "60%",
        resizeMode: "contain"
    },
    buttonList: {
        marginTop: 45
    },
    button: {
        marginBottom: 16
    },
    loggedInText: {
        color: "black",
        textAlign: "center",
        fontSize: 14
    }
});

export default styles;
